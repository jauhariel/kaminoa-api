# ── Salin ke .env lalu isi seperlunya ──
# cp .env.example .env
# Semua nilai OPSIONAL kecuali API_KEY (hanya kalau ada endpoint auth: true).
# Fitur yang butuh kredensial punya fallback sendiri, jadi API tetap jalan
# walau env di bawah kosong (mode terbatas / pakai default).

# ── Inti (index.js) ──
# Kunci untuk endpoint dengan auth: true. Dikirim client via header x-api-key.
# Kalau kosong, semua endpoint auth: true menolak request.
API_KEY=
# Port server (default 47291 bila kosong).
PORT=47291

# ── Instagram (/downloader/instagram2) ──
# Cookie login IG untuk unlock video_url HD + durasi + musik + play_count.
# WAJIB set cookie LENGKAP (bukan cuma sessionid+csrftoken) — kalau tidak, IG
# balikin 302 redirect-loop. Minimal: csrftoken, sessionid, datr, mid, ig_did, ds_user_id.
# Kalau kosong/expired, endpoint auto-fallback ke embed (metadata + thumbnail, tanpa video_url).
IG_COOKIE=csrftoken=...; sessionid=...; datr=...; mid=...; ig_did=...; ds_user_id=...

# ── Sakana AI (/ai/sakana) ──
# Session token override. Kalau kosong, endpoint auto-mint session sendiri via Firebase.
SAKANA_SESSION=

# ── Qwen (lib/qwen.js) ──
# Kredensial akun Qwen. Ada default hardcoded di kode, override di sini kalau
# mau pakai akun sendiri (disarankan, biar tidak berbagi rate-limit).
QWEN_EMAIL=
QWEN_PASSWORD=
